{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build debug cat",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "command": "make",
            "group": "build",
            "args": ["s21_cat_debug"]
        },
        {
            "label": "Build debug grep",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "command": "make",
            "group": "build",
            "args": ["s21_grep_debug"]
        }
    ]
}